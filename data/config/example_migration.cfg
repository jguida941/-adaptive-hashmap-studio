# Example: Force migration thresholds for dashboard demo
# Lower max_lf_chaining so migration happens ahead of chaining rehash.
# Set max_group_len tight so adversarial write bursts trigger.
max_lf_chaining=0.55
max_group_len=2
initial_buckets=64
incremental_batch=1024
start_backend=chaining
